<div class="panel-heading">
    <h1>{{ title }}</h1>
</div>

<div class="panel-body">
    {% if (hasAlerts) %}
        {{ firstAlert | raw }}
    {% endif %}

    {% block body %}{% endblock %}
</div>
